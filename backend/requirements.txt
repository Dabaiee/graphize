fastapi>=0.115
uvicorn[standard]>=0.34
# Unpinned: this app uses output_config.format (structured outputs) and
# claude-opus-4-8, which require a recent SDK. Always pull the latest at build.
anthropic
neo4j>=5.26
python-multipart>=0.0.20
pypdf>=5.1
python-docx>=1.1
